<%= @attraction.name %>
<%= button_to "Go on this ride", go_on_ride_path, params: {'attraction_id' => @attraction.id, 'user_id' => session[:user_id]} %>

<% if @user.admin == true%>
<%= @attraction.name %>
<%= button_to "Show #{@attraction.name}", edit_attraction_path(@attraction) %>
<% end %>
